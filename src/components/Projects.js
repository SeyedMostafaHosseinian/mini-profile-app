import React from 'react';
import Navbar from './Navbar';

const Projects = () => {
    return (
        <div>
            { <Navbar title="projects" /> }
            projects
        </div>
    );
};

export default Projects;