import React from 'react';
import Navbar from './Navbar';

const Call = () => {
    return (
        <div>
            {<Navbar title="call"/>}
           call 
        </div>
    );
};

export default Call;